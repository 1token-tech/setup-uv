name: CI

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup uv
        uses: 1token-tech/setup-uv@master

      - name: Verify setup
        run: |
          uv --version
          python --version
